[security]
disable_initial_admin_creation = false
admin_user = {{ with secret "secret/data/grafana" }}{{ .Data.data.USER_ADMIN_GRAFANA }}{{ end }}
admin_password = {{ with secret "secret/data/grafana" }}{{ .Data.data.MDP_ADMIN_GRAFANA }}{{ end }}

[server]
root_url = https://monitoring.localhost
serve_from_sub_path = true
